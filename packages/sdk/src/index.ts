/**
 * Public barrel for @oxyhq/crowdsource.
 *
 * The client — report creation with idempotency keys, presigned evidence
 * uploads, case and decision reads — is not written yet. This package exists as
 * the boundary it will be published from and deliberately exports nothing until
 * the API it wraps exists.
 */

export {};
