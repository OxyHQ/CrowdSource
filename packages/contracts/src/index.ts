/**
 * Public barrel for @oxyhq/crowdsource-contracts.
 *
 * The contract modules — case envelope, resources, taxonomy, policies, reviews,
 * decisions, webhooks and reputation events — are not written yet. This package
 * exists as the boundary they will be published from; it deliberately exports
 * nothing until a real schema lands, so no consumer can build against a shape
 * that has not been agreed.
 */

export {};
